# Set this to your deployed backend's origin (no trailing slash), e.g.
#   VITE_API_BASE_URL=https://claude-skills-hub-api.onrender.com
# Leave unset for local dev — Vite proxies /api to localhost:8000 instead.
VITE_API_BASE_URL=
